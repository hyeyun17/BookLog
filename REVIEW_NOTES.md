# 검토 및 수정 내용

현재 루트 프로젝트의 `app` 모듈을 수정했다. 하위 `BookLog/`는 별도 복사본으로 판단해 변경하지 않았다.
레이아웃, 색상, 테마, 화면 배치는 유지했다. XML에서는 기존 주석만 제거했다.

## 반영 사항

- ISBN을 문자열로 전달해 작성 화면에서 누락되지 않도록 수정했다.
- 검색 요청을 취소해 이전 응답이 최신 결과를 덮어쓰지 않도록 했다.
- 비동기 화면 갱신을 뷰 생명주기에 맞추고 바인딩과 지도 참조를 해제했다.
- 라이브러리, 타임라인, 지도가 DB 변경을 관찰하도록 했다.
- 필수 입력 검사, 중복 저장 방지, 작성 화면의 뒤로 가기 경로를 수정했다.
- 사진 선택을 OpenDocument로 변경하고 읽기 권한과 선택 상태를 유지했다.
- 모든 독후감 삭제 후 샘플이 다시 생성되던 자동 삽입을 제거했다. 기존 기록은 삭제하지 않았다.
- 실제 위치를 조회하며, 권한 거부나 조회 시간 초과 시 좌표 없이 저장한다.
- DB 2 → 3 마이그레이션으로 기존 기록을 보존하고 위치를 선택 값으로 변경했다.
- DB를 자동 삭제하는 마이그레이션 설정을 제거했다.
- API 키를 `local.properties` 또는 환경변수에서 읽도록 변경했다.
- 중복 Navigation 의존성을 제거하고 Java/Kotlin 대상 버전을 17로 통일했다.
- 기존 주석을 제거하고 필요한 이유만 간결하게 다시 작성했다.

## 로컬 설정

`local.properties`에 아래 항목을 설정한다. 기존 개발 환경의 값은 옮겨 두었다.

```properties
ALADIN_TTB_KEY=발급받은_알라딘_키
GOOGLE_MAPS_API_KEY=발급받은_지도_키
```

같은 이름의 환경변수가 있으면 우선 적용한다. 키 값은 문서와 저장소에 포함하지 않는다.
앱에 포함되는 키는 APK에서 추출 가능하므로 Google Cloud에서 패키지명과 서명 인증서 제한을 설정해야 한다.
하위 프로젝트 복사본과 과거에 공유한 파일의 키는 이번 수정 대상에 포함하지 않았다.

## 추가 확인 및 개선

- 실제 기기에서 사진 첨부 후 앱 재시작, 위치 권한 허용·거부, 작성 중 화면 회전을 확인한다.
- `ReviewDatabaseTest`로 저장·수정·삭제와 2 → 3 마이그레이션을 검증한다.
- 버전 1 DB 스키마는 제공되지 않아 1 → 3 마이그레이션을 추정해서 추가하지 않았다.
- 기존 기록의 서울시청 좌표는 실제 위치인지 구분할 수 없어 보존했다.
- 작성 중 프로세스 종료 및 저장 작업의 화면 회전 대응을 더 강화하려면 ViewModel과 SavedStateHandle로 상태와 작업을 분리한다.
- API 오류 응답 구분, 검색 페이지 처리, 결과 캐시를 추가하면 사용성이 개선된다.
- UI 변경 허용 시 접근성 설명, 입력 필드 설정, 문자열 리소스화 경고를 정리한다.

## 검증 명령

```powershell
.\gradlew.bat :app:assembleDebug :app:testDebugUnitTest :app:lintDebug :app:assembleDebugAndroidTest
.\gradlew.bat :app:connectedDebugAndroidTest
```

두 번째 명령은 연결된 기기 또는 에뮬레이터가 필요하다.

구현 참고: [Room 마이그레이션](https://developer.android.com/training/data-storage/room/migrating-db-versions), [현재 위치 조회](https://developers.google.com/android/reference/com/google/android/gms/location/FusedLocationProviderClient).

## ?? ??

- `ReviewWriteViewModel`? ?? ?? ??? ??? ????, ?? ? ??? ????? ??? ????.
- `SearchViewModel`? ???? ??? ?? ???? ????. ?? ??? ?? ??? ????.
- `BookRepository`? ?? ??? ???? ?? ?? ?? ? ?????.
- Room ??? 3?4? `app/schemas`? ????.
- ??????? ? ?? ??? ?? ? Lint ??? ??? ???. ?? ?? ??? ?? ??? ?????? ???? ???.

## ?? ?? ??

- ??? ?? ?? ??? APK ?? ??.
- ?? ??? 18? ??: ???????? ????? ????? ????DB ??????.
- Android 16 ????? ??? 5? ??: ?? ???, ?? ??, DB CRUD ? ??????.
- Lint ?? 0?, ?? 105?. ??? ???? ?? ???? ???.
- ?? ?? ?? UI ??? ??? SHA-256? ?? ????.
- ?? ?? ???? ??? ? URI ??? ?? ??? GPS ??? ????? ???? ???? ???.
- ??? ?????? ?? ???? ???? ?? ???? ?? ??? ????.
